# Demo project

Dump env when running git commit by install a commit-msg hook.

## Install hook

Run `install-githooks.sh` to setup hooks.


## Dump env

Run git commit in your AI tools, will dump env to file.
